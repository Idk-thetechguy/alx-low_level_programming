#include "main.h"
/**
* _islower - function that checks whether a character is lowercase
* @c: the character to be checked
* Return: 1 if c is a lowercase letter, 0 otherwise
*/
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
