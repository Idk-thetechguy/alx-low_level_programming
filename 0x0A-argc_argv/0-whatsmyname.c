#include <stdio.h>

/**
* main - prints the program's own name
* @argc: argument count
* @argv: array of pointers to strings
*
* Return: 0 on success
*/
int main(int argc, char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
